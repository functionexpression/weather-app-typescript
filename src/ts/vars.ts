export const svg = document.querySelector(".svg") as HTMLImageElement;
export const weatherDOM = document.getElementById(
  "weather"
) as HTMLTextAreaElement;
export const cityDOM = document.getElementById("city") as HTMLTextAreaElement;
export const degreesDOM = document.getElementById(
  "degrees"
) as HTMLTextAreaElement;
export const windDOM = document.getElementById("wind") as HTMLTextAreaElement;
export const humidityDOM = document.getElementById(
  "humidity"
) as HTMLTextAreaElement;
export const precipitationDOM = document.getElementById(
  "precipitation"
) as HTMLTextAreaElement;
export const cloudcoverDOM = document.getElementById(
  "cloudcover"
) as HTMLTextAreaElement;
export const errMsg = document.querySelector(".errmsg") as HTMLDivElement;
export const errMsgText = document.querySelector(
  ".errmsg-text"
) as HTMLTextAreaElement;
export const settingsIconDOM = document.querySelector(
  ".settings-icon"
) as HTMLDivElement;
export const settingsDOM = document.querySelector(
  ".settings"
) as HTMLDivElement;
export const toggleUnitsDOM = document.getElementById(
  "toggleUnits"
) as HTMLInputElement;
export const inputCityDOM = document.getElementById(
  "city-input"
) as HTMLTextAreaElement;
